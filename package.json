{
  "name": "api-nest-monorepo",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --parallel --filter frontend dev",
    "start": "pnpm --parallel --filter frontend start",
    "build": "pnpm --filter=frontend --filter=backend build",
    "lint": "pnpm --filter=frontend --filter=backend lint",
    "prepare": "husky install",
    "postinstall": "cd backend && go mod tidy"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^13.2.3"
  }
}
